<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/home.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Edu+AU+VIC+WA+NT+Hand:wght@400..700&family=Poiret+One&display=swap" rel="stylesheet">
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %> 
   
    <div class="container">
      <h1>Your Workouts</h1>

      <a class="btn btn-primary" href="/users/<%=user._id%>/workouts/new">Add Workout</a>

      <ul class="workout-list">
        <% workouts.forEach((workout) => { %>
        <li>
          <a href="/users/<%=user._id%>/workouts/<%=workout._id %>"><%=workout.type %></a>
        </li>
        <% }) %>
      </ul>

      
    </div>
  </body>
</html>